# [HASH] — /plan [verify-run-2026-08-31]: lifted above the readiness line, its blocker driven to done and confirmed by adb

Session date and time: 2026-09-05, afternoon planning run, at the opening's below-the-line revisit.

The audit that drives the twelve unrun checks from the 2026-08-31 run against a real phone had been held
against [install-current-build-on-device] since 2026-09-04. That item was driven to done the same day,
and its record shows why the hold could lift on more than a claim: Android Studio compiled without
deploying — the app launching was the previously installed build, confirmed by an unchanged install
timestamp in `dumpsys` — so Claude installed the APK over adb instead, needing `-t` because the
intermediates APK carries the test-only flag. Built and verified, not merely built, which is what the
lift rule asks for.

Recorded on the item so a later reader is not misled: the run that closed on 2026-09-05 shipped twelve
further builds whose ticks also read UNCONFIRMED. This item's list covers the 2026-08-31 run only, and
the newer ones are not folded into it.

Its red flag stays cleared, on the design already recorded: evidence from the phone stays in the session
scratchpad rather than this public repository, nothing from the device is written into a tracked file,
and TEST-LOG.md records verdicts rather than task content.

**Queue changes:** [verify-run-2026-08-31] moved above the readiness line and its `Blocked by:` line
removed, with what cleared it written into its prose. Later in the close it was placed after the
cleared block's build items, with the other work that stops for the user.

**Work processed:** none — this is a lift, not a disposition.
