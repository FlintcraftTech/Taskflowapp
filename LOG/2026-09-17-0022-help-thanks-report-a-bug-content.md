# [HASH] — /plan [0022-help-thanks-report-a-bug-content]: cut from three drawer screens to two when the Thanks screen was dropped

**Kind: processed.** Recorded 2026-09-17 at 16:01, read from the clock, during a planning session.

This item fills the bottom-of-drawer screens with real content. It had carried three since it was filed —
Help, Thanks and Report a bug. The Thanks screen was dropped from the product in this same session; the
reasoning is in the record for [remove-thanks-screen] and is not repeated here.

The item was rewritten rather than appended to, because the change overturns its scope rather than adding
to it: the heading, the held-below-the-line paragraph, the build block's Changes line and its Acceptance
line all counted three screens. The acceptance now also asserts the absence of the drawer row, so a build
checking this item cannot pass while the row is still there.

The slug keeps the word "thanks" in it. Slugs are stable through a rename by design, and renaming it would
break every reference and every record naming it.

**Queue changes:** [0022-help-thanks-report-a-bug-content] rewritten from three screens to two, heading
included; stays held below the line behind [help-thanks-report-content], which is unchanged by this.

**Work processed:** kept — [0022-help-thanks-report-a-bug-content], amended in place rather than
re-dispositioned.
