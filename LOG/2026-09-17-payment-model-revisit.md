# [HASH] — /plan [payment-model-revisit]: Alex asked for the payment model itself to be revisited, after a screen turned out to be the fossil of the last one

**Kind: processed.** Recorded 2026-09-17 at 16:01, read from the clock, during a planning session.

Filed on Alex's instruction, at the moment the Thanks screen was dropped. Her point: Taskflow has had at
least two payment models — donations, then the paid subscription now in SPEC §Tier model — and nothing in
the record shows that swap being weighed as a decision. The donation model simply stopped being mentioned,
and the only trace it left was a screen nobody could explain. The current model deserves an actual
discussion rather than inheriting the same silence. The Thanks-screen reasoning is in the record for
[remove-thanks-screen].

Held against [business-registration-for-play-account], and the choice of blocker is the part worth
recording. Alex asked for it to be held on "the relevant items", and the two obvious candidates —
[0017-tier-model-and-subscription-handling] and [play-console-subscription-product] — would have held
nothing: both sit in Processed, and a `Blocked by:` naming an entry already in Processed counts as
resolved, so either would have released this capture at the next planning opening. The business
registration is in Unprocessed and dated, so it genuinely holds. That reasoning is written into the
capture itself, including the two rejected candidates, so a later session does not repoint it back at them.

**Queue changes:** new capture [payment-model-revisit] appended to Unprocessed, held against
[business-registration-for-play-account].

**Work processed:** none — filed as a capture, not processed into work. Its own turn comes when the
business registration is.
