# [HASH] — Deleted after its content moved: the end-to-end test is held below the line against the outliner fix, on Alex's own condition

Recorded 2026-09-06, 00:29.

Alex deferred [first-end-to-end-test] during the 2026-09-05 /next run on her own condition — not until
subtasks exist. This capture existed to make that condition survive the conversation it was said in, and
its own text named the work as a `Blocked by:` line for /plan to add rather than something a build may
write.

So it was carried out here and the capture deleted. Two things had landed since it was written, both in
its favour. The item that ships subtasks now has a name and a shape — [edit-outliner-missing], cleared to
run, two files, a defect four characters wide — so "whichever item ships the subtask affordance" resolves
to something concrete. And the export step this capture also wanted folded in had already been written
into the same walkthrough minutes earlier.

**What this changes in the record is worth stating, because it reverses something.** The test item's own
prose called its ordering a preference rather than a blocker, and said plainly that nothing stopped it
being run earlier. Alex's condition supersedes that, on the same reasoning hardened by what the audit
found: a day of real tasks with no way to break one into steps would fill the notes with the absence of a
feature already known and already filed, which is the outcome that paragraph was written to avoid. The
item now carries both sentences, so the supersession is visible rather than silent.

The reinstall caveat the capture also mentioned needed nothing — the walkthrough's second step already
installs the current build.

**Queue changes:** [first-end-to-end-test] gained `Blocked by: [edit-outliner-missing]` and moved below
the readiness line; this capture deleted.
**Work processed:** deleted — [first-end-to-end-test-waits-on-subtasks], content relocated.
