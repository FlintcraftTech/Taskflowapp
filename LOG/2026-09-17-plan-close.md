# [HASH] — /plan chat-level record: fourteen entries processed, the cleared region 1 → 13, a screen traced back to a dead funding model, the roster redesigned, and a privacy risk designed out of the Play account

**Kind: processed.** Recorded 2026-09-17 at 16:01, read from the clock, during a planning session.

This session ran across 2026-09-12, 2026-09-16 and 2026-09-17.

The chat opened with one item cleared to run and that item unrunnable — a defect filed the same day had
stopped the only thing a build run could have taken. It closes with thirteen cleared, nothing left in
Unprocessed that is not dated out or held, and four things settled that had each been going round for
weeks.

The through-line worth naming is how often an entry's own account of the world turned out to be wrong, and
how cheap the check was. [rotating-roster-free-choice] named an obstacle that one grep disproved.
[tomorrow-task-lands-on-today-under-custom-boundary] named a diagnosis that was the reverse of the truth,
found by reading the file it told us to read. [strategy-paragraph-input-loses-characters] named its
strongest evidence, and that evidence was most likely a driver artifact while the real fault sat plainly in
two files. The Thanks screen's whole existence rested on a funding model nobody had recorded abandoning.
Each item's own record carries its correction.

**Advisory: not needed** — nothing is waiting to be processed. Every remaining Unprocessed entry is dated
out or held, so the next planning session has no queue to work; what the project has is thirteen cleared
items.

**Queue changes:** cleared region 1 → 13. Kept and cleared: [compile-rule-vs-multi-item-runs],
[tools-md-environment-facts], [verify-2026-09-12-code-on-device], [remove-thanks-screen],
[tomorrow-task-lands-on-today-under-custom-boundary], [roster-as-subtasks],
[recurring-completions-in-history], [strategy-paragraph-input-loses-characters],
[run-migration-test-on-emulator], [supabase-apply-0003-grants]. Lifted from the held region:
[subtask-affordance-in-edit-dialogue], [first-end-to-end-test]. Deleted:
[rotating-roster-free-choice], [duns-issued-to-sole-trader] (after relocation). Amended in place:
[0022-help-thanks-report-a-bug-content], [help-thanks-report-content],
[play-console-subscription-product], [business-registration-for-play-account],
[day-begins-at-rollover-still-unrun]. Filed as captures: [payment-model-revisit]. Completed and removed:
[duns-application-outcome]. SPEC edited in five places across §Recurring tasks, §Parent tasks
expand/collapse, §Search and completed history, §Settings and §Side menu.

**Work processed:** fourteen entries. Ten kept and cleared, two lifted, two deleted, one completed and
removed.

**Red flag cleared this session:** [duns-issued-to-sole-trader]'s, by designing it out rather than
accepting it — recorded in that item's own entry.

**Also in this chat:**

- **An update that was not an update, costing two app restarts.** The session opening advertised version
  1.22.0 on the stable channel against an installed test build. Alex restarted the app before the update
  commands had been run, on the expectation the notice set up; the commands then reported the installed
  version was already the latest. This machine installs from a local folder, so the notice was comparing
  against a channel the install route cannot reach. Filed as [tools-md-environment-facts] and reported.
- **Four messages sent, all shown in full and approved before sending.** Three method defect reports to the
  No code method project — the update notice above; `inbox_send.py` refusing a send because this project's
  address book was in a prose form its parser cannot read, which had caused two earlier messages to bypass
  the mailbox and gitignore checks entirely; and the undated-time-word check firing on Taskflow's own page
  names. One message to the flintcraft.tech project, telling it Taskflow's Play chain waits on its business
  registration and now has a condition on what gets registered. Each has a line in `INBOX/sent.md`.
- **flintcraft.tech recorded in the address book for the first time**, confirmed by Alex, and the address
  book given a table form alongside its prose so the send script can read it.
- **Mail triaged and archived:** a report from another project this user runs, saying a D-U-N-S application
  already existed. That produced [duns-application-outcome], which was then driven to completion.
- **Two corrections from Alex on how I was working**, both the same fault: I put a balanced choice to her
  where I had in fact reached a view, and she answered "as you recommend" — twice. The method requires
  recommending one option and letting the other be declined. The second time I named the fault before
  answering.
- **A Gmail search pulled unrelated personal correspondence into the transcript** — other people's names,
  addresses and phone numbers — while establishing that the connector could not see the account that
  mattered. None of it was written into any file, which was said plainly at the time. Taskflow's documents
  go to a public repository, and not writing it down is the only protection that check offers.
- **The undated-time-word safety check blocked two writes on Taskflow's own page names**, once in the queue
  and once in a log entry. Both passed on the retry. Reported.
- **The business-registration entry's October date was re-raised and deliberately kept.** Given the
  dependency chain behind it, three months was proposed; Alex kept mid-October, and the entry now records
  that so nobody asks a third time.

The records are checked against the scrub checklist and the credential-shape scan, and against nothing
more — neither can tell whether a sentence quietly identifies a real person.
