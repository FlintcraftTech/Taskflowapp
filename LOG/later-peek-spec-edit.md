# [HASH] — SPEC.md §Schedule view — replaced the collapsed-by-default Later-card sentence with peek behaviour (each card opens showing its first ~3 tasks, the rest behind the expand/collapse control), keeping the calm-overview-not-a-wall-of-tasks rationale; sets up the [later-card-peek] build. [later-peek-spec-edit]

The Later Project cards opened collapsed to just their header — the collapsed-by-default decision folded into SPEC §Schedule view on 2026-06-16 and kept through the Later-by-Project rewrite. Alex decided on 2026-06-22, during the [later-by-project-screen] device test, that Later should show some tasks at a glance rather than only Project headers. This spec edit reverses collapsed-by-default to a peek: each card opens showing its first ~3 tasks, with the rest behind the expand/collapse control. The number is ~3, not the ~7 first floated — ~7 per card across several Projects would rebuild the wall of tasks that collapsed-by-default existed to prevent, whereas ~3 is a glanceable peek that keeps Later a calm overview. The calm-overview rationale is preserved in the new wording. This is SPEC only; the LaterPage build follows in [later-card-peek], which depends on this edit. Kept separate from [polish-spec-edits] (which also edits §Schedule view) so the reversal carries its own clean LOG trail.

**Files touched:**
- SPEC.md — §Schedule view collapsed-by-default sentence replaced with peek behaviour

**Routed to Captures:** none
