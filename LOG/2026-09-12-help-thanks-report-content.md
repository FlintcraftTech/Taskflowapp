# d6ac7e8 — /plan [help-thanks-report-content]: the writable words folded into the build that consumes them, leaving a capture that names only what is genuinely stuck

This capture had been set aside three times — 2026-08-25, 2026-08-31, 2026-09-03 —
always because two of Help's topics describe a Claude setup path that does not
exist. It came back in front of Alex on a technicality: the plugin's rule for when
a held capture returns changed in 1.22.0-test5, from "the thing it waits on is
finished" to "has been written up". Its blockers have been written up and sit
below the readiness line behind a chain, so nothing about its situation had moved.

Rather than set it aside a fourth time, its shape was fixed. Ask what files it
changes and the answer is none — the words it produces have no home of their own,
they are destined for [0022-help-thanks-report-a-bug-content]'s build. So it was
undone design sitting outside the item that needs it.

Two pieces were fully writable and are now written into that build item: the
Report-a-bug screen in full, carrying `bugs@flintcraft.tech`, and the Help topic
explaining that tasks dated before today stay where you put them, worded without
naming the behaviour as a category, which SPEC requires. Refused: splitting those
into an item of their own — they produce no file, so they are design belonging
inside the build.

A correction was made mid-recommendation: three pieces were claimed writable and
only two are. The Thanks paragraph's content was never decided — nobody has said
who the app is thanking — so it stays in the capture, now recorded as waiting on a
decision Alex owns rather than on the Claude work.

**Queue changes:** rewritten and left in Unprocessed, still held, now naming three
things instead of five; [0022-help-thanks-report-a-bug-content] amended to carry
the settled wording and the reasoning behind the email route.

**Work processed:** none — the capture stays unprocessed, deliberately.
