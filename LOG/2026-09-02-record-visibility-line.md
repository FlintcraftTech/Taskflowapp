# [HASH] — build [record-visibility-line]: the repository's public visibility recorded as the user's informed choice

The 2026-08-31 migration added a `Visibility:` slot to CLAUDE.md's plugin-managed block and left it blank. This item filled it with the decision made in planning that same day.

**The decision, and the consent trail behind it.** The project's documents — SPEC.md, QUEUE.md and LOG/ — live in the public repository FlintCraftTech/Taskflowapp and are world-readable. The user was told plainly that the planning documents contain personal material, that an ignore rule added later would not untrack or scrub what is already committed, and that this method's own checks cannot certify the documents safe to publish. They chose to keep the repository public. Making it private was the offered alternative and lost because the user never wanted it private.

The line also records that visibility is not to be raised again as a risk. That is the point of writing it down: without it, every fresh session meets the same public repository holding the same personal material and surfaces it again, and the user answers the same question indefinitely. Changing it is theirs to ask for.

**Red flag:** this item carried one, and it was cleared — by informed consent rather than by design, which is one of the two ways the model allows. The substantive clearing record was written at the planning close that cleared it on 2026-08-31; this entry carries it through, as the item's shipping record.

**Rule gate:** run, and it passes as the item recorded it. The Visibility line is the slot the plugin-managed block carries for exactly this decision, the added text is one standing line the user approved in planning on 2026-08-31, and nothing is evicted or reworded elsewhere.

**Files touched:** CLAUDE.md — the `Visibility:` line in the plugin-managed block.

**Routed to Captures:** none.

**Tick:** done, confirmed: the line is no longer blank and states the public-by-choice decision with its date. Read back after writing.
