# [HASH] — /plan [test-log-owes-2026-09-06-rows]: the rule widened to cover any test outcome, not just a build's, before the rows get backfilled

TEST-LOG.md is this project's running test history, one row per test since May. The
run of 2026-09-06 produced more test results than any session so far and wrote none
of them.

The capture was right that the rows are not the interesting part. CLAUDE.md's rule
conditions recording on a build running tests, and that day's testing happened
inside an `[audit]` item and inside `[user]` walk-throughs — so the rule's trigger
and the run's shape never met. Writing the rows without settling that would move
the ambiguity into the file, where the next session guesses again.

Settled with Alex the wide way: the table records a test outcome whoever produced
it — a build, a review pass, or a step she performed on the device. The value of a
running history is being the one place that says what has actually been exercised,
and one that silently omits the checks a person ran is worse than none, because it
reads as complete. Refused: leaving the rule narrow and backfilling anyway.

It is a build rather than something planning could do: a planning session may not
write CLAUDE.md, and TEST-LOG.md is a project record rather than a planning
document. Everything needed is already in the 2026-09-06 entries, so nothing has to
be re-run.

**Queue changes:** kept into Processed, cleared to run, with both files named and
the observation stated as a grep that either returns the rows or does not.

**Work processed:** kept — [test-log-owes-2026-09-06-rows].
