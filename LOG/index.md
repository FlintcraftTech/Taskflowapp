# LOG Index

One-line summaries of each session, newest first; each line names the session's full entry file in this folder. Maintained by Claude — /done writes one entry per completed session. Not for cover-to-cover reading — search and scan when you need the "why" behind a previous session's choices.

The entry format is documented in the comment block below.

<!--
Entry format (newest first):

## <Session> — YYYY-MM-DD — One-line summary

**What shipped.** Short plain-English paragraph describing concrete deliverables. Reference TEST-LOG row range rather than restating test outcomes.

**Decisions taken and why.** Two or three bullets on load-bearing decisions — what was chosen, alternatives considered, what tipped the call. Skip housekeeping; focus on choices shaping future sessions.

**Pivots and surprises.** Anything that turned out differently than the plan expected — a bug, a wrong assumption, an external fact discovered mid-build.

**Carried forward.** Items raised but not done, with destination (which capture, QUEUE entry, or "not pursued — reason").
-->

- [HASH] — /plan, all 14 unprocessed entries worked: red flag [mcp-server-auth-model] CLEARED by settling the MCP connector auth model on OAuth with dynamic client registration (research filed at resources/research/claude-custom-connector-auth-options.md — URL-borne and pasted static keys both ruled out by the connector surface), folded into [0020-remote-mcp-server] and SPEC §Claude integration via remote MCP, capture deleted; deletions after relocating content — [search-feature] → [nav-left-spine-spec-edit] (read-only results, scope = all tasks + Project names, Strategy doc excluded), [subscription-pause-resume-merge] → [0018-cloud-sync-paid-tier] (resume takes the union, offline deletes don't propagate), [claude-md-project-rules-stale-vocabulary] → [claude-md-stale-vocabulary] (now all four stale refs, `Serves SYSTEM-PROMPT.md:` kept and reworded to work item), [bridge-asks-from-method-project] and [method-user-item-to-taskflow-handoff] answered and replied to by mailbox; SPEC gained the auth model, the "Claude acting on the user's instruction is not an external task app" line plus no-provenance-marker rule, the multi-device resume rule, additive import + per-task completion state, and a new §Focus on one Project temporarily; kept and cleared [onboarding-video-script], [empty-state-copy-and-visuals] (distinguishing copy, no counters on cards), [execute-by-task-area] (transient Later-card focus, dies on relaunch), [first-end-to-end-test]; held [custom-instruction-production-text], [onboarding-video-content] (both split Claude/user), [post-first-test-polish-review]; [nav-completed-history] and [help-thanks-report-content] skipped, [personal-strategy-preview] dated to 2026-11-25 → 2026-08-25-plan.md
- 3f06c56 — /setup migration 1.20.0-test14 → test18, no format conversion (epoch already 4): CLAUDE.md's PLUGIN-MANAGED block replaced marker-to-marker with the installed template — the stale Batches / Deferred tests / Parked / REGISTRY.md description gone, INBOX and /rescan in, the five Rules-for-Claude bullets replaced by the template's single SPEC rule; nothing user-authored sat inside the markers so nothing was relocated, and the Project rules below them are untouched; that swap is the first half of [claude-md-stale-vocabulary], which was amended (prose plus a narrowed build block) to say so and stays in Processed for the two remaining Project-rules references; new capture [claude-md-project-rules-stale-vocabulary] for further retired vocabulary in the TEST-LOG.md and SYSTEM-PROMPT.md rules that the existing item does not name; `Throughliner Brevity` output style offered and accepted, written to the gitignored .claude/settings.local.json; scaffold settings otherwise already reconciled; SPEC.md untouched and carries no stale "Project docs" section; red flag [mcp-server-auth-model] surfaced and left uncleared → 2026-08-25-setup.md
- f3f5668 — /setup migration, format 3 → 4: the epoch-4 conversion only — a `--- Build block ---` (Changes / Acceptance, plus `Refused:` on three) written beneath all 23 cleared build items in QUEUE.md, 6 lifted from the items' own prose and 17 from `archive/backlog-specs/` after the user chose that over shelving them below the readiness line; blocks name real source files where the code exists and cite the archived spec where it doesn't; [0012-settings-day-begins-at] absorbs [schedule-day-boundary-tick]'s boundary recompute and [0014-json-export-and-import] absorbs [bridge-asks-from-method-project]'s additive-import and completions-readout questions; BUILD-VIEW.md generated (24 cleared / 23 blocked, the 24th a `[user]` check needing none); no item reworded, moved or deleted and the readiness line unmoved; version markers set to 1.20.0-test14 and epoch 4; new uncleared red flag [mcp-server-auth-model] filed on the MCP server having no stated way to tie a request to a user; CLAUDE.md's retired vocabulary reported and left alone, already queued as [claude-md-stale-vocabulary] → 2026-08-21-setup.md
- 0772534 — /plan: corrected [claude-md-stale-vocabulary]'s premise (the stale text is inside CLAUDE.md's PLUGIN-MANAGED markers, plugin-owned rather than the user's wording) and kept it as a template-swap build; fixed SPEC's "each batch's spec" instance directly, and rewrote SPEC's opening paragraph which wrongly forbade writing an entry ahead of its build; split [nav-completed-history] at its seam into new kept item [nav-left-spine-spec-edit] (four SPEC sections for the spine's left half) plus the build remainder left in Unprocessed, after settling unified search-plus-completed-history and PNG-plus-Markdown share with the user; cross-referenced [search-feature] to fold in and be deleted; filed two research findings on Android share formats; drafted a method feedback report on /setup not refreshing the managed block; routed one mailbox message to [bridge-asks-from-method-project]; stopped on the format 3 → 4 drift → 2026-08-21-plan.md
- f2d1fc2 — Mail triage only, no build and no queue processing: a consumer project's report that a multi-part `[user]` work item has nowhere good to live became one capture in Unprocessed, [method-user-item-to-taskflow-handoff], framing the handoff of such parts to Taskflow tasks as Taskflow design work; the message's request that Taskflow raise it with the method project directly is left open, awaiting the user → 2026-08-19-inbox-triage.md
- 043e9d1 — /setup migration: QUEUE.md converted from the old six-section shape (Red flags / Batches / Parked / Deferred tests / Captures) to two-section Processed + Unprocessed, 40 items, nothing dropped — 22 cleared, 8 held with `Blocked by:` slugs, 11 unprocessed; deferred tests became `[user]` walkthroughs; INBOX mailbox added and gitignored; `.si-version` replaced by `.throughliner-version` + `.throughliner-format-epoch`; REGISTRY.md deleted as stale and derivable → 2026-08-19-setup.md
- f82e03d — ScheduleViewModel.kt `dateLabelFor` — return null for the Tomorrow slot so Tomorrow rows show no date label (redundant with the page title); KDoc updated to match. [tomorrow-no-date-label] → tomorrow-no-date-label.md
- d9de2c6 — SPEC.md §Schedule view — replaced the collapsed-by-default Later-card sentence with peek behaviour (each card opens showing its first ~3 tasks, the rest behind the expand/collapse control), keeping the calm-overview-not-a-wall-of-tasks rationale; sets up the [later-card-peek] build. [later-peek-spec-edit] → later-peek-spec-edit.md
- 913f0e9 — /plan: shelved the personal-strategy + Claude-memory dogfood idea as parked capture [personal-strategy-preview] (blocked on SI multi-spec), and folded the misleading empty-Later-card case into parked [Empty state copy and visuals] → plan-2026-06-24.md
- 988a9fa — SPEC.md §Schedule view — dropped Tomorrow from the DD/MM date-label rule, so only Soon and Later show dates; §Side menu — reframed the drawer opener from left-edge swipe to tapping the ☰ button, recording swipe-to-open as intentionally disabled to avoid colliding with the spine's horizontal-swipe navigation. [polish-spec-edits] → polish-spec-edits.md
- 5b68c5f — ScheduleScreen.kt spine header polish: fixed page-title overlap during transitions (the two titles slid past each other and crossed in the centre; first tried a full stagger but it read as two separate beats on device, so shipped an overlapping slide with a crossfade — 180ms each, incoming delayed 100ms) and swapped the text-glyph ← / → arrows for vertically-centred Material AutoMirrored KeyboardArrowLeft/Right icons; no gradle change (icons-core resolves transitively via material3) → spine-header-polish.md
- 3f841d5 — Wired Project creation into the editor's Project picker: "New Project" dropdown entry (below a divider) opens a name-only card; `EditTaskViewModel.createProjectAndSelect` creates the Project at max-sort+1 (+ empty Strategy entry) and selects it so the edited task files in on save. Build decision: relocated creation from the orphaned `AppViewModel.createProject` into the editor's view-model (which already held projectRepository + the projectId), then deleted the now-dead `AppViewModel`. Restores Project creation lost when [later-by-project-screen] removed the overview "+ New Project". Device-verified create/move/order; filed a capture on the misleading empty-Later-card copy → project-create-picker-ui.md
- 5c4d0e6 — /plan: promoted [project-create-picker-ui] to top of Build (wires the editor Project picker's missing "New Project" entry + name card to the orphaned `AppViewModel.createProject` — Project creation was impossible after the Later rebuild deleted the overview "+ New Project"); retired [project-lifecycle-later], splitting its reorder/delete remainder by dependency into [project-delete-later] (general bin gesture), [project-reorder-strategy] (0015 Strategy editor), [project-lifecycle-paid] (0020/0021 + SYSTEM-PROMPT.md); merged within-card Later reorder into [task-reorder-within-list] (one drag primitive across flat slots + nested cards) and fixed its stale rescope note; split the Later ~3-task peek into [later-peek-spec-edit] + [later-card-peek]; re-cast [execute-by-task-area] trigger-less (focus-mode floated + rejected); re-parked the Project-deletion data check as [verify-project-delete-data]; rolled 2 deferred device checks into [project-create-picker-ui] → plan-2026-06-23.md
- 5d549be — Later rebuilt as Project-grouped expand/collapse cards (new `ui/schedule/LaterPage.kt`; one card per Project in Strategy order, Unassigned pinned bottom, collapsed by default, empty cards shown; grouping moved into ScheduleViewModel — now includes never-slotted undated tasks the old flat Later bucket dropped); deleted the per-Project view (`ui/project/`) + Projects overview (`ui/projects/`) and their nav (Projects spine page, Project overlay + host, drawer per-Project list, destinations) + the orphaned `EditTarget.NewInProject`; FAB-on-Later → undated Unassigned task; within-card drag-reorder split to a capture (pairs with [task-reorder-within-list]); peek-3-default + create-path-gap captured (overview "+ New Project" gone, no Project creation until [project-lifecycle-later], now unblocked); two multi-Project device checks deferred → later-by-project-screen.md
- 781e60b — Data model — replaced nullable `Task.projectId` with a non-null FK defaulting to a seeded system "Unassigned" Project (`is_system` on Project.kt); destructive v2 migration in TaskflowDatabase.kt seeds Unassigned via `onOpen` (the seed-callback bug caught on-device — onCreate/onDestructiveMigration both wrong); ProjectRepository.delete reassigns a deleted Project's tasks to Unassigned (FK SET_NULL→RESTRICT); StrategyRepository excludes the system Project; ProjectDao/TaskDao gain system-fetch + bulk-reassign; +3 approved out-of-batch files (TaskflowApplication wiring, EditTaskViewModel null-coercion, TaskDaoTest seed+2 tests); 18/18 instrumentation tests pass on Pixel 6 → unassigned-project-model.md
- b6d732f — /plan: parked [project-lifecycle-later] behind [later-by-project-screen]; split the two hybrid polish batches into combined spec-edit [polish-spec-edits] + feature-only [tomorrow-no-date-label]/[disable-drawer-swipe-open]; resolved the queue-maintenance cleanup (de-numbered both batches' SPEC line-refs, reworked the parked "Empty state copy" item for the Later-card world) → plan-2026-06-22-2.md
- c70816b — SPEC.md — Later-by-Project rewrite: system "Unassigned" Project replaces null projectId; Later becomes Project-grouped expand/collapse cards (Projects overview + per-Project view removed, dreaming surface folded into the Later card); new §Create or delete a Project (creation via the editor's Project picker "New Project" entry, deletion by long-press drag-to-target with tasks → Unassigned); Strategy doc made the single source of Project order with tier-split reorder/delete (free direct, paid via Claude + "discuss high-level strategy" toast); §Side menu de-Projected; UX principles 3/5 + Completed-tray + Drag-target cross-refs realigned; build/SYSTEM-PROMPT follow-on captured as [project-lifecycle-later] → later-by-project-spec-edit.md
- 1cdd823 — /plan: pulled [later-by-project] off the post-core shelf and queued the Later-by-Project redesign as three top-of-Build batches — [later-by-project-spec-edit] (SPEC rewrite of 8 sections), [unassigned-project-model] (system "Unassigned" Project, non-null projectId + migration), [later-by-project-screen] (Project-grouped Later cards; deletes per-Project view + Projects overview; side-menu trimmed); resolved both 2026-06-21 device-test captures (Projects out of the menu; no off-spine Project surface), reversed an initial no-refile call (kept the editor Project picker, both tiers), rescoped [task-reorder-within-list] to Schedule-slots-only, dropped deferred tests [project-create→0004]/[0005], parked the execute-by-area question → plan-2026-06-22.md
- d0d4f89 — ProjectsOverviewScreen: added manual name-only Project creation — a "+ New Project" affordance + name dialog wired through ScheduleScreen/AppRoot to a new AppViewModel.createProject that inserts at next sort-order and creates the Project's empty Strategy entry; creation verified on a Pixel 6 (✓), the two folded-in Project-view checks ([0004]/[0005]) deferred after Alex raised two Project-navigation design objections (both → Captures) → project-create.md
- d52eb42 — /plan: parked [later-by-project] (merge the Later slot + Projects overview into one Project-grouped "Later" screen) trigger-less as a post-core right-half-spine design thread, after deciding it does NOT block the top batch [project-create] — that batch's creation plumbing is merge-independent, only the add-button location would move; folded the decision plus two design findings (the Project view survives the merge; the "removes redundancy" framing corrected) into the parked capture. → plan-2026-06-20-2.md
- 62852c2 — SPEC.md — §Projects overview gains manual name-only Project creation (an add affordance that asks only for a name; the new Project is appended to the end of the Projects sort order and mirrored in the side menu + Strategy doc; no description captured here — that's the later Strategy-doc paragraph). §Add a new task narrows "from any screen" to the real task-add surfaces (the four Schedule slots + a Project view) and states the Projects-overview affordance creates a Project, not a task. → project-create-spec-edit.md
- f3bbb5b — /plan: promoted two device-check captures into [tomorrow-no-date-label] (drop the redundant Tomorrow DD/MM label; spec-edit §Schedule view + dateLabelFor) and [disable-drawer-swipe-open] (gesturesEnabled=false in AppRoot, ☰ as sole menu opener, resolving drawer-swipe vs spine-swipe collision; spec-edit §Side menu), and rolled the runnable blank-New-task-form check into test batch [verify-blank-new-task-form]; all three in the polish cluster after [spine-header-polish] → plan-2026-06-20.md
- 55d7165 — Add-flow create-path fixes — fixed the stale New-task title (EditTaskScreen.kt: per-slot ViewModel key in the Activity store replaced with a dialogue-scoped ViewModelStore cleared on dispose, so each FAB open opens a fresh blank form), and closed the Today-slot "stores tomorrow's date" bug as not-a-bug — verified correct by live device test plus static proof (a Today-add stores noon today, which can't cross a day), no code change; filed a capture proposing Tomorrow drop its date label → add-flow-create-path-fixes.md
- f90693b — /plan: promoted the two device-verify create-path bugs into one build batch [add-flow-create-path-fixes] at the top of Build (Today-add stores tomorrow's date — capture's date-logic diagnosis found wrong on a full read, scoped reproduce-then-fix; New-task title persists — root-caused to the per-slot ViewModel key), and dropped the stale device-verify reshape capture as already-actioned → plan-2026-06-19-2.md
- df15c34 — [device-verify-core-screens] device-verified the FAB-reachable add/render/complete loop on a Pixel 6 (0002 + 0005 non-Project): all passed except two create-path bugs — Today slot stamps tomorrow's date (task lands on Tomorrow, not Today) and the New-task form keeps the previous title; both routed to Captures for a fix-batch → device-verify-core-screens-2.md
- 6847887 — /plan: untangled the aborted device-verify batch and the project-creation gap — added [project-create-spec-edit] + [project-create] (name-only Project creation, folding in the §Add spec-drift), rescoped [device-verify-core-screens] to runnable-now FAB-reachable checks (Project checks → [project-create] Test, date-matrix → Deferred tests behind 0006), promoted [task-reorder-within-list] + [spine-header-polish], and kept Projects as screens after rejecting a projects-abstract/search-only redesign → plan-2026-06-19.md
- 604934a — /next test (aborted) — opened [device-verify-core-screens] to run the deferred 0002/0004/0005 device checks, found the batch's "FAB seeds everything" premise false, aborted and requeued it, and filed two captures (reshape direction + project-creation gap) → device-verify-core-screens.md
- f613461 — /plan — rolled the now-runnable deferred device tests (batches 0002 Schedule, 0004 Project, 0005 add/edit/complete) into one Test batch [device-verify-core-screens] placed at the top of Batches ahead of 0006, and emptied the Deferred tests section (the three lines moved into the new batch); three captures (spine-header-polish, task-reorder-by-drag, §Add "from any screen" spec-drift) left unprocessed for a later /plan. → plan-2026-06-18-3.md
- 14149bd — 0005 — add-task-and-edit-task-minimum-viable (batch 0005) — added task creation (a Scaffold FAB driving a capture-inherits-context add flow), a new edit dialogue (`ui/edit/` — EditTaskScreen + EditTaskViewModel: title, notes, editable Project incl. unassigned, read-only date), tap-to-complete on non-parent rows across both Schedule (SlotPage) and Project views, and a basic completed tray on Today; wired through ScheduleScreen/ScheduleViewModel, ProjectScreen/ProjectViewModel, and AppRoot. Recurrence deferred to 0007 (no recurrence column), date-editing to 0006, tray auto-clear to 0012. → 0005-add-task-and-edit-task.md
- f2079e4 — Project view (batch 0004) — built the Project screen (new `ui/project/` — ProjectScreen + ProjectViewModel) replacing the placeholder overlay: a collapsible "currently scheduled" card (collapsed by default, dated tasks in read-only Schedule order) over a below-card list of undated tasks, with three plain empty states; render-only per the 0002/0003 pattern, with completion-from-card deferred to 0005 and within-list drag-reorder deferred (captured, no batch home yet). → 0004-project-view.md
- 569a655 — Side menu + Projects overview (batch 0003) — built the left-edge navigation drawer as a single spine-mirroring list (Today·Tomorrow·Soon·Later, Projects in order, Strategy) with app-actions pinned at the bottom, added the ☰ menu key to the spine header, and added the Projects overview spine page right of Later; a new ui/navigation/ shell (AppRoot drawer host + AppViewModel projects flow) drives the spine pager and opens placeholder overlays for Project/Strategy/app-action taps, with Project drag-reorder deferred to the Strategy-doc work (0015). → 0003-side-menu-schedule-projects-app-actions.md
- c667d53 — TEST-LOG.md — added rows #012–017 recording the six ProjectDao instrumentation tests (create, update, delete, ordered retrieval, reorder persistence, max-sort-order) run on the Pixel 6, closing the Project half of the batch-0001 data-foundation verification left unrun since 0001. → projectdao-instrumentation-tests.md
- f53921c — /plan: promoted the ProjectDao instrumentation-test capture into a new Test batch [projectdao-instrumentation-tests] at the top of Batches — run the six ProjectDaoTest tests on the Pixel 6 and record TEST-LOG #012–017; cleared the sole unprocessed capture. → plan-2026-06-18-2.md
- 5ae750d — /next test — ran the ten batch-0001 TaskDao instrumentation tests on the connected Pixel 6: all 10 passed, flipping TEST-LOG #002–011 from Skipped to Pass and verifying the data foundation (date round-trip, undated Soon/Later parking, Project assign + refile, slot + Project reorder persistence, subtask link, parent completion roll-up). → taskdao-instrumentation-tests.md
- 68987f7 — /plan: queued a test batch for the batch 0001 TaskDao tests (deferred-test roll, now runnable on the connected Pixel 6), parked the day-boundary re-bucketing capture behind 0012, and removed the stale "Run /plan before building" note from the Batches heading. → plan-2026-06-18.md
- 21d89e5 — SPEC.md — rewrote navigation into a left-to-right spine (Today · Tomorrow · Soon · Later · Projects · Strategy): principle 3 reframed (four slots as the spine's left segment, reachable by swipe), §Schedule view re-described as pages on the spine and stripped of date-row + rollover-placement build mechanics (absorbs trim findings F3/F4), §Side menu + §Projects-in-the-side-menu merged into one spine-mirroring navigation list with the Strategy row moved to after the projects list (absorbs F18), §Strategy-doc row repositioned, and a Projects overview spine page added right of Later. → nav-spine-spec-edit.md
- 00b8b8b — Schedule view (batch 0002) — built the read-only Today/Tomorrow/Soon/Later screens as a HorizontalPager (spine's left segment, Today default) with date-derived slot placement (new domain/SlotDeriver + 11 passing unit tests, 4 AM day-start constant pending 0012), per-slot empty states, and a new TaskDao/TaskRepository active-tasks Flow; header redesigned mid-build (user-directed) from an all-tabs row to a single centred current-slot name that slides in the swipe direction, flanked by tappable ←/→ chevrons that hide at dead ends, corners left clear for the future menu key + pick-up delete target; verified on a Pixel 6. → 0002-schedule-view-date-derived-placement.md
- 65769ab — /plan: committed to the navigation-spine redesign — promoted the right half to a new spec-edit batch [nav-spine-spec-edit] at the top of Build (rewrites SPEC principle 3 + §Schedule view + §Side menu + §Strategy doc into the spine, adds a Projects overview spine page, absorbs held trim findings F3/F4/F18, moves the Strategy-doc menu row to after the projects list), reframed the 0002/0003/0004 summaries as spine work, and re-parked the left half (completed-history) as [nav-completed-history] (was [nav-zoom-spine-and-completed-history]). → plan-2026-06-17-2.md
- 6fcd9d6 — SPEC.md — trimmed exhaustive UI/implementation mechanics to concept-level across ~10 sections (Project card scroll-bound, drag-to-reschedule gestures + per-drop dates, date-strip fade/dimensions/no-date styling, parent expand-collapse + outliner keystrokes, drag-target placement/hover, cut-paste flow, completed tray, onboarding card copy), reworded the intro to drop the "every UI element" mandate, replaced projectId-null jargon with "Project defaults to unassigned", deduped the date-set-moves-task rule to its one home in §Move between Schedule and Project, folded in completed-task retention (retained not deleted at tray rollover); verified relocated detail lives in archived batch specs and backfilled F9 strip dimensions into 0006. F3/F4/F18 held for the nav redesign; F20 no-edit. → spec-edit-trim.md
- a069cb1 — /plan: decided the 20-finding SPEC-trim audit and queued batch [spec-edit-trim] at the top of Build (trim exhaustive UI mechanics to concept-level, relocated detail homed in the archived batch specs; F2 keeps slot thresholds, F20 no-edit, F3/F4/F18 held for the nav redesign); removed the plan marker; resolved the completed-task tray-fate as retain-not-delete (unblocks 0005); parked the new [nav-zoom-spine-and-completed-history] redesign thread. → plan-2026-06-17.md
- 34ccf66 — Audit SPEC.md against keep-vs-relocate criteria — routed 20 per-section findings to Captures (no SPEC edits) → audit-spec-trim.md
- 86d521b — SPEC.md — folded 2026-06-16 planning decisions into seven sections: Strategy doc added as a calm top row of the Projects side-menu section (above individual Projects); Project-view top card collapsed by default (expand opt-in); date-strip gained 5 details (centre-on-date open, ~1mo-past–12mo-future range + month-jump, capped-linear fade, left-edge labelled "no date" tile, 5–7 tiles visible); Day-begins-at default set to 4:00 AM; completed tray clears at the day-begins-at rollover; paused subscription reverts to local-only with device DB as source of truth; new scope note — no notifications in v1 (foreground-only, per principles 4 & 7). → spec-edit-planning-2026-06-16.md
- 1e4b29a — Migrated docs to the Sovereign Implementer vocabulary (UX→SPEC, BACKLOG→QUEUE, MANIFEST→REGISTRY, build-log→LOG; docs to project root; FAQ/ + .si-version added; CLAUDE.md rewritten) and ran a planning pass: 18 carried-over questions → 3 batches (spec-edit of 7 SPEC decisions; SPEC-trim audit + plan marker; SYSTEM-PROMPT reconciliation rule), 6 parks, 4 drops, 1 new capture; verified build compiles (TEST-LOG #001 Fail→Pass). → migration-and-plan-2026-06-16.md
- `001-project-skeleton-and-room-data-model.md` — 0001 — 2026-05-24 — Project skeleton and Room data model

---
*No-code method — Version 55.*
