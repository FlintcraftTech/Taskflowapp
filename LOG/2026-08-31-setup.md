# b28ee0e — /setup migration to format 5: resources/ moved into workshop/, managed CLAUDE.md block gains the Visibility slot

The installed plugin moved to 1.21.1-test3 and format 5 while this project sat on format 4, so the session opened on the out-of-date halt and ran the migration before any planning. The epoch-5 conversion is a folder move, not a queue conversion: the project's research notes moved from `resources/` to `workshop/resources/` with `git mv`, so their history follows, and the five QUEUE.md lines citing the old path were re-pointed. The plugin-managed block in CLAUDE.md was one section behind the shipped template and gained the Visibility slot (blank at that point; its filling became this session's [record-visibility-line]). Version marker set to 1.21.1-test3, format marker to 5, written last, after the conversion completed. Everything else checked clean: no retired terms in CLAUDE.md, no stale "Project docs" section in SPEC, ignore lines and the brevity style already in place, no REGISTRY.md, both section preambles already blockquoted.

**Queue changes:** the five `resources/research/` citations re-pointed to `workshop/resources/research/`; no item moved or reworded.
**Work processed:** none — migration only.
