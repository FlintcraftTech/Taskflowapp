# 9e24ba7 — /plan [verify-run-2026-08-31]: twelve device checks turned from the user's evening into Claude's work, after a capability check found the device reachable all along

The item listed twelve unverified areas from the run that closed on 2026-09-02 and asked, in its own
closing line, whether it should become several items. The answer turned out to be a different question.

TOOLS.md stated flatly that Claude cannot compile, test or install this app. The decision step's
capability check tested that rather than trusting it, and found it true only of Gradle: `adb` sits in
the Android SDK's `platform-tools` — not on PATH, which is why it read as absent — a phone answered
`adb devices` over wireless debugging, and `com.example.taskflow` was installed on it. So Claude can
tap, type, screenshot, restart the app and inspect state, and almost every observable on the list is a
pass/fail check rather than a judgement.

The item became an `[audit]`. What came out of its scope is stated in it: how anything *looks* stays
with [first-end-to-end-test], which already asks the user to note anything slow, confusing, ugly or
surprising. Two items for one job would make them do it twice.

A red flag was raised and cleared by design. Driving the phone means Claude sees real tasks in
screenshots, and this repository is public, so a committed screenshot would publish whatever was on
screen. Cleared by keeping evidence in the session scratchpad, outside the repository; writing nothing
from the device into a tracked file; never copying the database off the phone; and recording verdicts
rather than task content in TEST-LOG.md.

Splitting it into twelve items was refused — the checks share one app session and one setup, so twelve
items would pay that cost twelve times.

**Queue changes:** [verify-run-2026-08-31] reshaped from a `[user]` item to an `[audit]`, moved into
Processed below the line, and held against the new [install-current-build-on-device]. Red-flag marker
added, state cleared.

**Work processed:** kept, held below the line — [verify-run-2026-08-31].
