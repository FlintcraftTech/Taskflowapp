# [HASH] — /plan [help-thanks-report-content]: held until its blockers are BUILT rather than merely agreed, after a fourth deferral for the same reason

**Kind: processed.** Recorded 2026-09-17 at 16:01, read from the clock, during a planning session.

What remains of this entry is two pieces of Help-screen wording — how a user sets Claude up, and the
production version of the suggested custom-instruction text — and neither can be written before
[0019-ai-choice-flow-and-mcp-setup] and [0020-remote-mcp-server] define the path. The third thing it owed,
the Thanks screen's wording, is gone with the screen; that reasoning is in the record for
[remove-thanks-screen].

It had been set aside four times for the same reason, and part of that was not its fault. Its hold names
the two Claude items, and those sit in Processed — where, until the installed docset changed, a hold naming
an entry that had reached Processed counted as satisfied. So the entry was released and re-offered at every
planning opening however often it was deferred. This project reported that to the method's own project as
a defect on 2026-09-12.

The installed version answers it: a hold may end `until built`, which passes the entry over until every
named entry has a build record. This is the first item here to use it. Nothing else about the entry
changed — it keeps everything it has learned and returns by itself the day the MCP server ships.

Worth recording as a general observation: the item looked like a candidate for deletion each time it came
round, and the thing actually wrong was the shape of its hold rather than the work.

**Queue changes:** [help-thanks-report-content]'s `Blocked by:` line now ends `until built`, with a dated
paragraph explaining the change. It stays in Unprocessed.

**Work processed:** none kept and none deleted — the entry stays a capture, with its hold corrected.
