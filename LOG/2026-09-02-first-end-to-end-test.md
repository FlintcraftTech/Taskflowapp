# 3defa43 — presented and deferred during the /next run of 2026-08-31

Reached on 2026-09-02, on the walk-through pass that closed that run. Presented in full rather than
filtered out, and deferred on the user's own word.

## Precondition, checked on the item's own turn

Step 1 — install the current build — was already satisfied: the build was compiled and installed on
2026-09-02 while driving [verify-blank-new-task-form], which carries that record. The remaining
steps need a day of ordinary use to pass, so there was nothing a session could drive.

## What the user said, and why it matters to the item

They observed that this item is exactly the shape of work Taskflow's own paid-tier integration
exists to carry: a task generated inside a Claude Code session that has to be done away from it,
broken into steps, so that stepping away does not interrupt the flow it came from. The onboarding
video script written earlier in this same run demonstrates that case on its page 3.

They then reached for a workaround — defer the item and put a forward advisory on the next planning
session so it asks whether the test has been done — and named it themselves as the opposite of what
`[user]` items are for, and as behaviour they had worked hard to design out.

That reading is correct, and the method refuses the nag explicitly: its `[user]` lifecycle leaves the
gap open "precisely so nobody later notices the hole and proposes an ask to fill it". No advisory was
filed.

What was filed instead is [first-test-notes-observable]: this item uses none of the three routes the
method gives for knowing a `[user]` item is complete, because its last step stores both the notes and
the fact of completion in the user's memory. Naming a file for the notes gives it the third route —
an observable a later session can simply look for — and also gives [post-first-test-polish-review],
which is held against this item, somewhere to read its only input from.

## Outcome

**deferred** — the user's own word, given on 2026-09-02 when asked directly. Not started. The item
stays in Processed with its walkthrough unchanged; whether it gains a notes file is
[first-test-notes-observable]'s question for planning.
