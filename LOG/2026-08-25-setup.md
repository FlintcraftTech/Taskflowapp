# [HASH] — /setup migration test14 → test18: CLAUDE.md's plugin-managed block refreshed, the brevity output style enabled, and [claude-md-stale-vocabulary] halved by the refresh

The project was set up on plugin 1.20.0-test14 and the installed plugin is now 1.20.0-test18, so /setup ran as a migration. The document format was already current — the recorded epoch and the plugin's own are both 4 — so no conversion ran and no queue or spec content changed shape.

The substantive change is the plugin-managed block at the top of `CLAUDE.md`. That region is method-owned text the marker promises /setup keeps current, and it had gone stale: it described the queue as Red flags / Batches with Build-Test-Audit sub-headings / Deferred tests / Captures, mentioned `Parked:` headers and a `--- Plan session here ---` marker, listed the deleted `REGISTRY.md` as a project doc, and described /next as executing "the top batch". Since `CLAUDE.md` is read at the start of every session, every session was reading that description as current. The region was replaced with the installed template's, verbatim, marker to marker. Nothing user-authored sat inside the markers, so nothing had to be relocated below the end marker, and the Project rules section beneath it is untouched.

This is the same work [claude-md-stale-vocabulary] was queued to do, which is worth stating plainly rather than leaving to be discovered: the item asked for the marker-to-marker swap plus two fixes in the user-owned Project rules, and the swap is the half /setup performs by design. The item was amended on the user's agreement to say the swap has landed and that only the two Project-rules references remain, with its build block narrowed to match. It stays in Processed because real work is left in it. Doing the remaining two edits during the setup run was not the alternative considered and rejected — /setup's own rule is to report retired vocabulary in the user's text and edit nothing, because the wording is the user's to choose.

Reading `CLAUDE.md` for retired terms also turned up staleness the queued item does not name: the `TEST-LOG.md` rule describes the table as one row per shipped build batch and mentions a "test batch", and the `SYSTEM-PROMPT.md` rule ends with "Build batches that change its domain carry a `Serves SYSTEM-PROMPT.md:` line". Both describe machinery that no longer exists — batches are retired, and so is the test flavor. That became its own capture rather than being folded into the existing item, because what those rules should say instead is a wording decision rather than a mechanical swap; the `Serves SYSTEM-PROMPT.md:` convention in particular may still be wanted, attached to a work item rather than a batch.

The settings attached to the scaffold were reconciled and found already in place: `INBOX/` and `.throughliner/` are both ignored, and the FAQ, research and mailbox folders all exist. The one setting missing was an output style, this project having been set up before the style shipped. The offer was made and accepted, so `Throughliner Brevity` is now set in the project's `.claude/settings.local.json`; it takes effect at the next session or `/clear`, never mid-conversation. That file sits under the gitignored `.claude/` directory, so the setting is local to this machine and is not committed.

SPEC.md was neither read against nor edited — nothing this session did changed what the product is. `SPEC.md` also carries no "Project docs" section, so the stale-scaffold check that looks for one found nothing.

The queue's uncleared red flag was surfaced at the session's opening as it should be: the remote MCP server still has no decided way to tie an incoming request to a user, so nothing yet stops one user's tasks being read or changed through another's request. It stays uncleared and stays in Unprocessed — this session flagged it and decided nothing.

This entry and the capture written this session were read against the scrub checklist and the hook's credential-shape scan, and against nothing more; neither can tell whether a sentence quietly identifies a real person.

**Queue changes:** [claude-md-stale-vocabulary] amended — a paragraph and a narrowed build block recording that the managed-block swap landed here. One capture appended to Unprocessed, [claude-md-project-rules-stale-vocabulary]. One advisory filed in the reserved slot.

**Work processed:** none — no capture was kept or deleted this session. Processing is /plan's.

**Also in this chat:** the working tree already held changes from earlier sessions, folded into this commit: the automatic hash backfill on `LOG/index.md` and `LOG/2026-08-21-setup.md`, and the deletion of the previous `[forward-advisory]` capture from Unprocessed by a planning session that read it and never closed.

**Advisory: filed — forward-advisory**

**Routed to Captures:** [claude-md-project-rules-stale-vocabulary]
