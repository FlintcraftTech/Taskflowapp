# [HASH] — /plan [project-delete-later-premise-may-be-gone]: deleted after its question was answered — [project-delete-later] re-held and the sentence that wrongly cleared it rewritten

The capture existed because [project-delete-later] had been lifted into the
cleared region on an argument that turned out to be false: that the drag gesture
it needs "exists and has been driven on a real phone". What had been proven was a
vertical drag reordering within a list; what the item needs is delivery of a card
onto a target, which is the motion that fails.

It offered two options — hold the item and correct its prose, or return it to
Unprocessed. The gesture question was settled in this same session (see
[drag-eaten-by-page-swipe]), which spent the second option: the failure is a known
defect with a designed fix, so there is nothing left to wait for except that fix.

Alex delegated the call on whether the item's own design still stands. It does:
long-press-and-drag-to-a-target is SPEC's deletion gesture throughout, and what
failed was the target row sharing an axis with the page turn, not the route to
deletion. Refused: reconsidering how a Project is deleted — that would be
redesigning around a bug already being fixed.

**Queue changes:** deleted, every part relocated — the correction into
[project-delete-later]'s own prose, which no longer claims the gesture is proven,
and which is now held below the line against [drag-eaten-by-page-swipe] and
carries a note that its file list still has to be designed.

**Work processed:** deleted — [project-delete-later-premise-may-be-gone].
