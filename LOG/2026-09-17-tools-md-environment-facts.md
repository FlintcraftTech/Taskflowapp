# [HASH] — /plan [tools-md-environment-facts]: two things this session learned the hard way, queued for TOOLS.md so no session rediscovers them

**Kind: processed.** Recorded 2026-09-17 at 16:01, read from the clock, during a planning session.

Filed by the session's own look-back, from two facts established at cost rather than told.

**The plugin installs from a local folder.** `known_marketplaces.json` records the marketplace with source
type `directory`, pointing at the `throughliner/` folder inside the No code method project. So the only
version this install can reach is whatever that folder holds, and a session-start notice announcing a newer
version on a stable channel is comparing against something the install route never reads. The cost of not
knowing: Alex restarted the app twice on 2026-09-16 on the expectation that notice set up, and the update
commands, when finally run, answered that the installed version was already the latest.

**The Gmail connector reaches her everyday account only.** Established by searching it for the D-U-N-S
correspondence and finding no such thread, which is why forwarding rather than reconnecting was the route
in [duns-application-outcome]. Whether a second Google account can be added to a Claude connector was not
looked up and stays unknown rather than ruled out.

Both were already reported to the method's own project. This item is a different job: Taskflow keeping them
where its own sessions read them. Queued rather than written in the session because a planning session may
not write TOOLS.md.

**Queue changes:** new work item [tools-md-environment-facts], cleared to run — one file, `TOOLS.md`, no
compile.

**Work processed:** kept — [tools-md-environment-facts].
