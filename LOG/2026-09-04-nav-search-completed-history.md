# 9e24ba7 — /plan [nav-search-completed-history]: the Search page, the spine's new leftmost, with a dated list of completed tasks beneath it

One of four items split out of [nav-completed-history] this session; the split's reasoning is in
`2026-09-04-nav-completed-history.md` rather than restated here.

It follows [nav-yesterday-page] because both edit the same two files and that item makes
`SpinePage.slot` nullable — an ordering preference written into the item's prose rather than a
`Blocked by:` line, so the entry stays visible in the queue rather than being passed over.

Scoping search to the current page or Project was refused, on SPEC's own reasoning: a task someone is
searching for is one they have lost, so the app's organising principles are exactly what they cannot
use at that moment.

It also picks up the Search menu row folded in from [side-menu-spine-mismatch] — see
`2026-09-04-side-menu-spine-mismatch.md`.

**Queue changes:** [nav-search-completed-history] filed and moved into Processed, cleared to run.

**Work processed:** kept, cleared to run — [nav-search-completed-history].
