# 913f0e9 — /plan: shelved the personal-strategy + Claude-memory dogfood idea as parked capture [personal-strategy-preview] (blocked on SI multi-spec), and folded the misleading empty-Later-card case into parked [Empty state copy and visuals]

This planning session processed one discussion thread Alex raised and one filed capture; no batches changed.

Alex floated using this workspace as an early, live preview of Taskflow's personal Strategy-doc experience — keeping a real personal Strategy doc and having the strategy conversations here with Claude directly, before Taskflow and its remote MCP exist, plus syncing her Claude memory across surfaces and keeping her real tasks safe from Taskflow test-build wipes. The blocker is structural: SI is one-spec-per-project, and two of the three threads (the personal strategy practice, the memory sync) aren't Taskflow app features, so they don't fit SPEC.md's contract that every entry describes something existing in the build. Two ways to bend SPEC now were weighed and set aside — widening SPEC's framing from "Taskflow the product" to "this workspace's three concerns," and giving the personal threads their own sibling doc — because Alex chose to wait for SI to gain multi-spec handling rather than dilute the product spec now. Parked as a new capture with the full idea preserved, including the memory insight (point every Claude at one canonical strategy doc and have them read from it, rather than pushing updates into many memory stores) and a privacy note (personal data committed into this product repo would leak if the repo is ever shared).

The filed capture — a Later Project card reading "nothing in this project yet" when the Project's only task is near-term dated and lives on the schedule rather than the card — was folded into the already-parked [Empty state copy and visuals] item as a fourth empty-state case. It's the same surface and the same copy decision as the other empty Later states, best decided together against the real screen, so a standalone item would only risk drift. Carried forward with both candidate fixes, flagging that the "near-term count on the card" option is broader than empty-state copy (it changes every card) and is really a card-design question.

**Queue changes:**
- No batches added, removed, or reordered.
- Parked capture [Empty state copy and visuals] gained a fourth empty-state case (near-term-dated empty Later card) with two candidate fixes.

**Captures routed:**
- Parked: new capture [personal-strategy-preview] (Blocked by: SI multi-spec handling) — the personal-strategy / memory dogfood idea.
- Folded + removed: the misleading-empty-Later-card capture → merged into parked [Empty state copy and visuals], standalone capture deleted.
