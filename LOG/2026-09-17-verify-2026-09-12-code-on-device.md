# [HASH] — /plan [verify-2026-09-12-code-on-device]: cut from four device checks to three, and given numbered steps it did not have

**Kind: processed.** Recorded 2026-09-17 at 16:01, read from the clock, during a planning session.

Four changes shipped on 2026-09-12 and compiled, and none had been watched running. Three items sit held
behind two of them, and none can honestly be released on a compile alone — which makes this the cleared
item that unblocks the most.

The fourth check was the rotating roster advancing on completion. Alex replaced that design in this same
session ([roster-as-subtasks]), so proving the old behaviour correct is phone time spent on something on
its way out. Cut to three: the Project-heading drag in the Strategy doc, the drag targets not being eaten
by the page turn, and the day-detail card opened from a search result. That overturns the item's own
reasoning that all four deserved one sitting — the reasoning was right when written.

The item carried its four observations as a prose paragraph and no steps. It now carries eight numbered
steps with what to look for at each, which is what a run reaching it unattended needs; the observations
themselves are unchanged, taken from each original item's own Observation line.

Kept: the refusal to split this into separate items. They are one sitting with one phone, and separate
items mean separate connect-and-unlock cycles for checks that take minutes each.

Alex connected her phone during the session for exactly this, and it was not driven before the session
closed — the run is still to happen.

**Queue changes:** [verify-2026-09-12-code-on-device] rewritten from a capture into an `[audit]` work item
and cleared to run, with the roster limb removed and numbered steps added.

**Work processed:** kept — [verify-2026-09-12-code-on-device].
